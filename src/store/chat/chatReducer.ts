const initialState = {
  isLoading: false,
  error: null,
};

export const chatReducer = (state = initialState, action: any) =>{
  switch (action.type) {
    case value:

      break;

    default:
      break;
  }
}
